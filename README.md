# nuthon

## subdirectories (?)
`git remote add source-repo <url-of-source-repo>`

### setup a new subdirectory

`git submodule add <url-of-child-repo> <path/to/folder>`   
`git commit -m "Add child repository as submodule"`   
`git push` 